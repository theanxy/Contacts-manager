Add card

<%= form_for @card do |f| %>
	<% if @card.errors.any? %>
	<ul>
		<% @card.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
	</ul>
	<% end %>
	<p>
		<%= f.label :name, "Name:" %><br>
		<%= f.text_field :name %>
	</p>
	<%= f.submit "Add"%>
<% end %>